{
  "name": "slimefront",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "build": "node build.js --mode=production",
    "dev": "node build.js --mode=development --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}