{
  "name": "slimefront",
  "version": "0.9.0",
  "private": true,
  "description": "SLIMEFRONT — juego web de defensa contra oleadas. Desarrollo: index.html carga src/*.js directo via <script> tags (sin bundler). Para publicar (itch.io), build.js genera un dist/ minificado y con console.* removido.",
  "scripts": {
    "build": "node build.js",
    "test": "node --test tests/unit/*.test.js tests/smoke/*.test.js",
    "test:watch": "node --test --watch tests/unit/*.test.js tests/smoke/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
